---
phase: prompt-sequence
task: 5
total_tasks: 7
status: ready-for-next
last_updated: 2026-03-27T13:00:00.000Z
---

# TalkTrack — Session Handoff

## Current Position

**Prompt sequence progress:** 4 of 7 complete
- ✅ Prompt 01: Foundation (Earcons, StateOrb, Hints)
- ✅ Prompt 02: Zero-Tap Enforcement (Recognition Lifecycle)
- ✅ Prompt 03: Voice Selection + ElevenLabs
- ✅ Prompt 04: VoiceBox + Playback Controls
- ⏳ Prompt 05: Volume, Bookmarks, Scores, Timer — NOT STARTED
- ⏳ Prompt 06: Sentence/Paragraph Modes
- ⏳ Prompt 07: Final Triple Audit

**Last commit:** Pending — feat: VoiceBox playback controller with progress tracking (prompt 04)

## Completed This Session (Prompt 04)

1. **VoiceBox Playback Controller** (`src/lib/speech/voicebox.ts`)
   - Unified playback interface wrapping synthesis.ts
   - Progress tracking (currentTime, duration, position, remainingTime)
   - Sentence-level tracking for Web Speech API
   - Duration estimation from word count
   - Progress callbacks for UI updates
   - Pause/resume with time tracking
   - formatTime() and formatRemaining() helpers

2. **PlaybackIndicator Component** (`src/components/rehearsal/PlaybackIndicator.tsx`)
   - Real-time progress bar with smooth animation
   - Time remaining display ("Xm Ys left")
   - Sentence dots for visual progress (optional)
   - Compact mode for inline use
   - PlaybackBar subcomponent for minimal display

3. **ListenMode Integration**
   - Replaced speak() with voicebox.play()
   - Added PlaybackIndicator with sentence dots below StateOrb
   - Shows progress during note playback

4. **PromptMode Integration**
   - Replaced speak() with voicebox.play()
   - Added compact PlaybackIndicator during TTS
   - Works for both title and reveal playback

5. **TestMode Integration**
   - Replaced speak() with voicebox.play()
   - Added compact PlaybackIndicator during TTS
   - Works for both prompt and help playback

## What's Working

- All 3 modes now show real-time playback progress
- Sentence-level tracking for Web Speech API
- Time remaining estimates
- Glanceable progress during hands-free use
- All previous features (voice commands, earcons, ElevenLabs)

## Files Created

- `src/lib/speech/voicebox.ts` — Unified playback controller

## Files Changed

- `src/components/rehearsal/PlaybackIndicator.tsx` — **NEW** progress UI
- `src/components/rehearsal/index.ts` — Export new component
- `src/components/rehearsal/ListenMode.tsx` — Use voicebox + indicator
- `src/components/rehearsal/PromptMode.tsx` — Use voicebox + indicator
- `src/components/rehearsal/TestMode.tsx` — Use voicebox + indicator

## Architecture Note

VoiceBox wraps synthesis.ts rather than replacing it:
```
voicebox.play() → synthesis.speak() → Web Speech API / ElevenLabs
```

This maintains backward compatibility while adding progress tracking.

## Next Action

Start Prompt 05: Volume, Bookmarks, Scores, Timer

This adds:
- Volume controls for TTS and earcons
- Slide bookmarks (mark for extra practice)
- Score history visualization
- Improved timer with actual vs estimated comparison

## Resume Command

```
/clear
```
Then load prompt 05 spec and execute.
