---
current_work: Phase 3 - Capacitor Migration
status: paused
last_updated: 2026-03-31T17:15:00Z
---

<current_state>
Phase 3 (Capacitor Migration) started. Capacitor initialized but Android platform not yet added.
</current_state>

<completed_work>

**This Session:**
1. Phase 4B complete - i18n wiring into components
2. Phase 5 complete - AI Coach wired into CompletionScreen
3. Phase 6B complete - Product Hunt launch kit
4. Phase 6C complete - Community outreach kit
5. Phase 3 started - Capacitor installed and configured

**Commits:**
- `b855b9e` - mark Phase 6 complete
- `73c24eb` - community outreach kit (Phase 6C)
- `b24763d` - Product Hunt launch kit (Phase 6B)
- `a079d7a` - i18n + AI Coach wiring
- Plus 4 earlier commits (Voice Intelligence, Audio Devices, PWA, etc.)

**Phase 3 Progress:**
- [x] Install @capacitor/core, @capacitor/cli, @capacitor/android
- [x] Run `npx cap init TalkTrack com.talktrack.app`
- [x] Configure capacitor.config.ts (loads from Vercel URL)
- [ ] Add Android platform (`npx cap add android`)
- [ ] Configure AndroidManifest.xml (permissions)
- [ ] Create foreground service for audio keep-alive
- [ ] Implement wake lock for screen-on
- [ ] Build and test APK

</completed_work>

<remaining_work>

**Phase 3 Tasks (in order):**
1. `npx cap add android` - creates android/ directory
2. Configure AndroidManifest.xml:
   - FOREGROUND_SERVICE permission
   - WAKE_LOCK permission
   - Service declaration
3. Install plugins:
   - @capacitor-community/keep-awake (screen on)
   - Foreground service plugin or custom native code
4. Create JS bridge to start/stop foreground service
5. Wire into rehearsal lifecycle (start service on session start, stop on end)
6. Build debug APK: `npx cap sync && cd android && ./gradlew assembleDebug`
7. Test on real device - verify audio continues when screen locks

**Key Decision Made:**
- Using "server URL" approach - Capacitor loads from Vercel
- This keeps API routes working (/api/coach, /api/extract, etc.)
- Native plugins add foreground service + wake lock

</remaining_work>

<decisions_made>

- Capacitor loads from https://talktrack-three.vercel.app (not static export)
- This preserves API routes while adding native capabilities
- Package name: com.talktrack.app (same as TWA)

</decisions_made>

<blockers>
None - ready to continue
</blockers>

<context>
TalkTrack is a voice-first PWA for rehearsing presentations.
Phase 3 adds Capacitor native shell to solve "audio stops on screen lock".
The key features needed are foreground service and wake lock.
</context>

<next_action>
When resuming:
1. Run `npx cap add android`
2. Install @capacitor-community/keep-awake
3. Research/install foreground service plugin
4. Configure Android permissions
5. Build and test
</next_action>
